export interface pokeMonInterface {
  id: number;
  name: string;
  sprites: {
    front_default: string;
  };
}

export interface pokeMonObjectInterface {
  id: number;
  name: string;
  sprite_url: string;
}
